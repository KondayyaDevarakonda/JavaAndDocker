import React from 'react';
import styled from 'styled-components';

const GridWrapper = styled.div`
  margin-top: 1em;
  margin-left: 12em;
  margin-right: 6em;
`;

export const About = (props) => (
  <GridWrapper>
    <p>This React implementation demonstrates the integration of Java Spring Boot Rest Api and Asp.Net Core Web API</p>
  </GridWrapper>
)